@{
    ViewBag.Title = "Home Page";
    //Layout = "MyLayout.cshtml";
}
<!--
<div class="jumbotron">
    <h1>Banco Santander</h1>
    <p class="lead">Banco Santander offers you all services for managing your money online.</p>
    <p><a href="https://asp.net" class="btn btn-primary btn-lg">Powered by ASP.NET</a></p>
</div>
-->
<div class="row">
    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-large btn-block">
            <span class="glyphicon glyphicon-arrow-down"> Withdrawal</span>
        </a>
        <a href="#" class="btn btn-primary btn-large btn-block">
            <span class="glyphicon glyphicon-flash"> Quick cash $100</span>
        </a>
        <a href="@Url.Action("Details","CheckingAccount")" class="btn btn-primary btn-large btn-block">
            <span class="glyphicon glyphicon-question-sign"> Balance Inquiry</span>
        </a>
    </div>
    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-large btn-block">
            <span class="glyphicon glyphicon-print"> Print Statement</span>
        </a>
        <a href="#" class="btn btn-primary btn-large btn-block">
            <span class="glyphicon glyphicon-transfer"> Transfer Funds</span>
        </a>
        <a href="@Url.Action("Deposit", "Transaction", new{checkingAccountId=ViewBag.CheckingAccountId})" class="btn btn-primary btn-large btn-block">
            <span class="glyphicon glyphicon-arrow-up"> Deposit</span>
        </a>
    </div>
</div>

@section scripts{
    <script>var testing = true;</script>
}

@*
    You are using @Request.Browser.Browser, and that's OK! Email us at support@mvcatm.com if you have any problems. Or send us a tweet @@mvcatm.

    @{ var amounts = new List<Decimal> { 100, 25.50m, -40, 275.99m };}

    <ul>
        @foreach(decimal amount in amounts)
        {
            <li>@formatAmount(amount)></li>
        }
    </ul>

    @helper formatAmount(decimal amount)
    {
    var color = "green";
    if (amount < 0)
    {
        color = "red";
    }
        <span style="color:@color">@String.Format("{0:c}", amount)</span>
    }
*@